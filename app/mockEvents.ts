export const MOCK_EVENTS = [
  "Terminal: writing a regex to parse the changelog",
  "Terminal: regex failed — matched the entire file, including the license",
  "Terminal: googling 'how to exit vim' for the fourth time today",
  "Terminal: npm run build failed with 147 TypeScript errors",
  "Terminal: commented out the failing test and called it a fix",
  "Terminal: git push --force origin main",
  "Terminal: rm -rf node_modules && npm install (the ancient ritual)",
  "Terminal: it works on my machine, shipping to prod"
] as const;
